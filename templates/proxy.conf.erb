server {
	listen <% if ip.include? ":" %>[<%= ip %>]<% else %><%= ip %><% end %><%= server_port %>;
	<% if not cert_ip.empty? %>listen <%= cert_ip %>:443;
	listen [::<%= cert_id %>]:443;
	<% end %>
	server_name <%= name %>;
	location / {
		proxy_pass http://worker-<%= style %>;
	}
}
